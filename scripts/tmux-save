#!/usr/bin/env bash
# Save active tmux sessions (name + working directory) for restoration after reboot.
# Sessions are saved as "name|path" lines in ~/.tmux-sessions.

tmux list-sessions -F '#{session_name}|#{session_path}' 2>/dev/null > "$HOME/.tmux-sessions"
