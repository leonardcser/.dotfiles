return {
	"mg979/vim-visual-multi",
	version = "*", -- Use the latest tagged version
	opts = {
		pre_hook = function()
			require("nvim-autopairs").disable()
		end,

		post_hook = function()
			require("nvim-autopairs").enable()
		end,
	},
	config = function()
		-- Add any custom settings or leave it empty if no specific config is required
	end,
}
